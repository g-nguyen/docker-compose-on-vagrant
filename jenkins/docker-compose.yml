version: "3"
services:
  jenkins:
    container_name: jenkins
    image: jenkins/jenkins:lts
    ports:
      - 8080:8080
      - 5000:5000
    environment:
      JENKINS_HOME: "/vagrant/jenkins/jenkins_home"
    volumes:
      - ../:/vagrant
